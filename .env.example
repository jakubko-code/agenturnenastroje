# NextAuth
AUTH_SECRET=replace-with-long-random-secret
AUTH_GOOGLE_ID=replace-google-client-id
AUTH_GOOGLE_SECRET=replace-google-client-secret
AUTH_TRUST_HOST=true

# Comma-separated emails and/or a domain restriction
ALLOWED_EMAILS=owner@agentura.sk
ALLOWED_DOMAIN=agentura.sk

# Prisma
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agenturne_ai

# Encryption key for provider keys (32 bytes base64)
ENCRYPTION_KEY_BASE64=replace-with-32-byte-base64

# Optional shared provider keys (fallback if DB keys not set)
OPENAI_API_KEY=
GEMINI_API_KEY=
CLAUDE_API_KEY=

# Optional pricing config (USD per 1M tokens) for cost estimation in history
PRICING_OPENAI_INPUT_PER_1M_USD=
PRICING_OPENAI_OUTPUT_PER_1M_USD=
PRICING_GEMINI_INPUT_PER_1M_USD=
PRICING_GEMINI_OUTPUT_PER_1M_USD=
PRICING_CLAUDE_INPUT_PER_1M_USD=
PRICING_CLAUDE_OUTPUT_PER_1M_USD=

# Retention / archiving job
RETENTION_DAYS=90
ARCHIVE_BATCH_SIZE=250
ARCHIVE_MAX_BATCHES=50
ARCHIVE_DRY_RUN=false
